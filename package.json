{
  "name": "pern-todo",
  "version": "1.0.0",
  "description": "React + Postgres + Express + Node",
  "main": "index.js",
  "type": "module",
  "dependencies": {
    "cross-env": "^7.0.3",
    "dotenv": "^10.0.0",
    "prettier": "^2.4.1"
  },
  "devDependencies": {
    "@types/jest": "^27.0.3",
    "concurrently": "^7.0.0",
    "eslint": "^7.32.0",
    "eslint-config-prettier": "^8.3.0",
    "jest": "^27.4.4"
  },
  "scripts": {
    "start-server": "cd server && npm run start",
    "start-client": "cd client && npm run start",
    "start": "concurrently \"npm run start-server\" \"npm run start-client\"",
    "test": "echo \"Error: no test specified\" && exit 1",
    "format": "npx prettier --write .",
    "format-check": "npx prettier --check",
    "lint": "npx eslint .",
    "postinstall": "(cd client && npm install); (cd server && npm install)"
  },
  "repository": {
    "type": "git",
    "url": "git+https://github.com/mdabbagh/pern-todo.git"
  },
  "author": "",
  "license": "ISC",
  "bugs": {
    "url": "https://github.com/mdabbagh/pern-todo/issues"
  },
  "homepage": "https://github.com/mdabbagh/pern-todo#readme"
}
